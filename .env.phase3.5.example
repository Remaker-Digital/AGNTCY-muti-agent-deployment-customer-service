# Phase 3.5 - AI Model Optimization Environment Configuration
# Copy this file to .env.phase3.5 and fill in your values

# =============================================================================
# Azure OpenAI Configuration
# =============================================================================

# Your Azure OpenAI resource endpoint
# Format: https://YOUR-RESOURCE-NAME.openai.azure.com/
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/

# Your Azure OpenAI API key
# Found in Azure Portal > Your OpenAI Resource > Keys and Endpoint
AZURE_OPENAI_API_KEY=your-api-key-here

# API version (use latest stable)
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# =============================================================================
# Model Deployments
# =============================================================================
# These are the deployment NAMES you created in Azure OpenAI Studio
# Not the model names - your custom deployment names

# GPT-4o-mini deployment (for intent, escalation, critic)
AZURE_OPENAI_GPT4O_MINI_DEPLOYMENT=gpt-4o-mini

# GPT-4o deployment (for response generation)
AZURE_OPENAI_GPT4O_DEPLOYMENT=gpt-4o

# Text embedding deployment (for RAG)
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-large

# =============================================================================
# Budget Settings
# =============================================================================

# Monthly budget limit in USD (Phase 3.5 target: $20-50)
PHASE35_BUDGET_LIMIT=50.00

# Alert threshold (0.80 = alert at 80% of budget)
PHASE35_ALERT_THRESHOLD=0.80

# =============================================================================
# Notes
# =============================================================================
#
# To set up Azure OpenAI:
# 1. Create Azure subscription (if you don't have one)
# 2. Request access to Azure OpenAI at: https://aka.ms/oai/access
# 3. Create Azure OpenAI resource in Azure Portal
# 4. Deploy models in Azure OpenAI Studio:
#    - GPT-4o-mini
#    - GPT-4o
#    - text-embedding-3-large
# 5. Copy endpoint and API key from Azure Portal
#
# Cost estimates for Phase 3.5:
# - GPT-4o-mini: ~$0.15/1M input, ~$0.60/1M output tokens
# - GPT-4o: ~$2.50/1M input, ~$10.00/1M output tokens
# - Embeddings: ~$0.13/1M tokens
#
# With 100 evaluation conversations, expect ~$10-30 total testing cost.
